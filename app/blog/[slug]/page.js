

export default function BlogPostPage({params}) {
//   const { slug } = params;

  return (
    <main>
      <h1>Blog Post</h1>
      {/* <p>This is the content for the blog post with slug: {slug}</p> */}
    </main>
  );
}
